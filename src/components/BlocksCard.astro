---

interface Props {
    target: string
}

const {target} = Astro.props
---

  <div class="animate-fade-in-down relative group hover:scale-98 overflow-hidden rounded-xl shadow-md transition-all duration-300 border border-neutral-800 hover:border-neutral-950">
    <article class="bg-neutral-900/10 p-3 rounded-xl">
        <slot/>
    </article>
    <button data-modal-target={`modal-${target}`} class="cursor-pointer flex justify-center items-center opacity-0 group-hover:opacity-100 transition-all duration-300 absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(0,0,0,0.1)_0%,rgba(0,0,0,0.40)_70%,rgba(0,0,0,1)_100%)]">
        
          <apan
      class="z-10 hover:scale-105 relative inline-flex transistion duration-300 overflow-hidden rounded-md p-0.5 "
    >
        <span
          class="absolute will-change-transform backface-hidden motion-reduce:animate-none blur-xs inset-[-1000%] animate-[spin_2s_linear_infinite] bg-[conic-gradient(from_90deg_at_50%_50%,#0f172a_0%,#1e293b_40%,#334155_70%,#0c4a6e_100%)]"
        >
        </span>
        <span
          class="inline-flex h-full w-full cursor-pointer items-center justify-center rounded-md bg-zinc-950/80 px-7 py-2 text-xs font-medium text-gray-300 backdrop-blur-3xl gap-2 undefined"
        >
          View details
        </span>
      </apan>
              <span class="z-0 absolute rounded-full bg-zinc-950/90 blur-2xl h-[200px] w-[200px]"></span>
    </button>

    <div class="-z-10 absolute inset-0  overflow-hidden">
  <div class="absolute inset-0 opacity-5 dark:opacity-10">
    <svg width="100%" height="100%">
      <pattern id="field-pattern" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
        <path
          d="M0 20H40M20 0V40"
          stroke="currentColor"
          stroke-width="0.5"
          class="text-green-500"
        />
      </pattern>
      <rect width="100%" height="100%" fill="url(#field-pattern)" />
    </svg>
  </div>
</div>
  </div>